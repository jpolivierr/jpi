pathLayouts: 
  - id: "domain"
    paths:
      - "/application"
      - "/interface"

packages:
  controller: "controller"
  service: "service"
  repository: "repository"
  mapper: "mapper"

packageGroups:
  - id: "layered"
    packages:
      - controller
      - service
      - repository
      - mapper
